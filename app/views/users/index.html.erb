<% if !@users.blank? %>
  <% for user in @users %>
    <%= user.name %><br/>
		<%= user.description %><br/>
		<%= image_tag(user.image) %><br/>
		<%= link_to "user's profile", user_path(user) %><br />
  <% end %>
<% else %>
  There are no users
<% end %>